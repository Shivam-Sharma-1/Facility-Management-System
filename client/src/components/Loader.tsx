const Loader = (): JSX.Element => {
  return (
    <div className="w-full h-full min-h-[100dvh] flex justify-center items-center">
      <span className="loader" />
    </div>
  );
};

export default Loader;
